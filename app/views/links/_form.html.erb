<%= form_with(model: link) do |form| %>
  <% if link.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(link.errors.count, "error") %> prohibited this link from being saved:</h2>

      <ul>
        <% link.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :original %>
    <%= form.text_field :original %>
  </div>

  <div class="field">
    <%= label_tag(:type, "Seleccione el tipo de link") %>
    <%= form.select :type, options_for_select(Category.all.map { |c| [c.name] }) %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
